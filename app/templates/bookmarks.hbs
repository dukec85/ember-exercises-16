import Ember from 'ember';

export default Ember.Controller.extend({
  actions
})

<form {{action "addBookmark" on="submit"}}>
  <label>Bookmark</label>
  {{input class="bookmark-submit" type="text" value=bookmark}}
<button>submit</button>
</form>
{{#each model as |mark|}}
  <h3>{{mark.bookmark}}</h3>
{{/each}}
{{outlet}}
